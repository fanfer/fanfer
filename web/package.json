{
  "name": "fanfer-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --outDir dist && node build-data.js && vite build --ssr src/main-ssr.js --outDir dist-ssr && node build-ssr.js",
    "preview": "vite preview --outDir dist"
  },
  "dependencies": {
    "highlight.js": "^11.9.0",
    "katex": "^0.16.45",
    "marked": "^11.0.0",
    "vue": "^3.4.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "@vue/server-renderer": "^3.4.0",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0",
    "vite": "^5.0.0"
  }
}
